import { ReactNode } from 'react';
export interface IToolbarProps {
    children?: ReactNode;
}
declare const Toolbar: ({ children }: IToolbarProps) => import("react/jsx-runtime").JSX.Element;
export { Toolbar };
