import { type ILayoutConfig } from '@/providers';

// Defining the layout configuration specific to demo10 layout
const Demo10LayoutConfig: ILayoutConfig = {
  name: 'demo10-layout', // Unique name identifier for this layout
  options: {}
};

export { Demo10LayoutConfig };
